name: learn-github-actions
on: 
  push:
  workflow_dispatch:
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      #- uses: actions/checkout@v2
      #- uses: actions/setup-node@v2
      #  with:
      #    node-version: '14'
      #- run: npm install -g bats
      #- run: bats -v
      #- uses: getsentry/action-git-diff-suggestions@main
      #  with:
      #    message: 'Testing diff'
      #- uses: actions/checkout@v2
      #- uses: actions/setup-java@v2
      #  with:
      #    distribution: 'adopt' # See 'Supported distributions' for available options
      #    java-version: '11'
      #- run: javac *.java
      #- run: ls -la
      #- run: java -cp . HelloWorld
      - run: echo 'My interaction 02'
      - uses: actions/checkout@v2
      - uses: zolaton34/zdocs@main
        id: hello-nice-world
        with:
          who-to-greet: Toutou
      - run: echo "${{ steps.hello-nice-world.outputs.random-number }}"
      #- uses: ./.github/actions/hello-world-action
      #- name: First interaction 01
        #with:
        #  # Token for the repository. Can be passed in using {{ secrets.GITHUB_TOKEN }}
        #  repo-token: ${{ secrets.GITHUB_TOKEN }}
        #  # Comment to post on an individual's first issue
        #  issue-message: "This is your first issue"
        #  # Comment to post on an individual's first pull request
        #  pr-message: "This your first pull request"

